/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/wheel-of-song-ideas", // <=== used by github pages
  output: "export",  // <=== enables static exports
  reactStrictMode: true,
};

export default nextConfig;
